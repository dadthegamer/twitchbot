import React, { useState } from 'react';
import Announce from './SubActions/Announce';

function EditAction({ action, onActionEdited }) {
    const [actionType, setActionType] = useState(action.type);
}
